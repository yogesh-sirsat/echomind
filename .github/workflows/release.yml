name: Release EchoMind

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  goreleaser:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'
          cache: true

      - name: Install ALSA (Linux Only)
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get install -y libasound2-dev

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: latest
          # --split tells GoReleaser to only build what the current runner is capable of
          args: ${{ startsWith(github.ref, 'refs/tags/') && 'release --clean --split' || 'build --snapshot --clean --single-target' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
